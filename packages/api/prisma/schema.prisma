generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
  // ! url is NOT here anymore – it lives in prisma.config.ts
}

model HealthCheck {
  id        String   @id @default(cuid())
  checkedAt DateTime @default(now())
}
