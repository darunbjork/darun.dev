app = "darun-ai-portfolio"
primary_region = "ams"

[build]
  dockerfile = "packages/api/Dockerfile"

[env]
  NODE_ENV = "production"
  PORT = "8080"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

  [[http_service.checks]]
    interval = "15s"
    timeout = "5s"
    grace_period = "45s"
    method = "GET"
    path = "/health"

  # TCP check — opens a raw socket to the port
  [[http_service.checks]]
    interval = "10s"
    timeout = "5s"
    grace_period = "30s"
    type = "tcp"
    port = 8080

[[vm]]
  size = "shared-cpu-1x"
  memory = "1gb"